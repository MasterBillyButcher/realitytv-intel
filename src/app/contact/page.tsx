export const metadata = { title: "Contact — RealityTV Intel" };

export default function ContactPage() {
  return (
    <div className="mx-auto max-w-3xl px-5 py-14">
      <h1 className="text-3xl mb-6">Contact</h1>
      <p className="text-sm" style={{ color: "var(--ink-soft)" }}>
        Replace this page with your actual contact details before deploying: an email address, a contact
        form, or a link to wherever inquiries should go. None is set here because none was provided.
      </p>
    </div>
  );
}
